export const layoutClasses = {
  page: "ds-page",
  section: "ds-stack",
  sectionHeading: "ds-section-heading",
  buttonGroup: "ds-button-group",
  grid: "ds-grid",
} as const;

export const surfaceClasses = {
  card: "ds-card",
  cardTitle: "ds-card-title",
  cardSubtitle: "ds-card-subtitle",
} as const;

export const formClasses = {
  form: "ds-form",
  formGrid: "ds-form-grid",
  field: "ds-field",
  label: "ds-label",
  help: "ds-help",
  error: "ds-error",
} as const;

export const tableClasses = {
  wrapper: "ds-table-wrap",
  table: "ds-table",
} as const;
