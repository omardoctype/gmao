apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: gmao
  labels:
    app: backend
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  selector:
    app: backend
